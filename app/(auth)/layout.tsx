/**
 * Auth layout – strips the storefront header/footer and mobile nav.
 * The login/register pages are full-screen standalone pages.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
